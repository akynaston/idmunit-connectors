We decided not to include the jar files for the DXCMD connector here since there are multiple versions of the jar files; and the ones used should match the version of IDM being used.

To make things eaiser, a batch file has been included to retrieve these batch files from a corresponding Linux based server.

